N Body Problem simulation in 2D using Three.js.

Running:

- pnpm install
- pnpm dev

Notes:

- Vite is used as a local dev server for ES modules and browser-friendly serving.
- Three.js is imported directly from CDN in source files.
